[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Redirect for clean URLs on Netlify
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
